# values-dev.yaml
image:
  repository: devd/babbel-task:dev-1753027676
  # tag: dev-latest  # will be replaced by GitHub Actions
  pullPolicy: IfNotPresent

env:
  ENVIRONMENT: dev
  DB_FILE: "/app/urls-dev.db" # This is not prod so keeping the state locally as this can be changed for prod
  AUTH_USER: "devuser"
  AUTH_PASSWORD: "devpass"

